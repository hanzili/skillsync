components:
  schemas:
    Forum:
      type: object
      properties:
        roadmap:
          type: string
          format: objectId
        threads:
          type: array
          items:
            type: string
            format: objectId
